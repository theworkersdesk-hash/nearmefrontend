include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_const_constructors: true
    avoid_print: true
    # require_trailing_commas intentionally omitted — it conflicts with
    # `dart format`'s line-wrapping and produces noise without value.

analyzer:
  errors:
    invalid_annotation_target: ignore
